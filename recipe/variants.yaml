mpi:
  - nompi
  - mpich
  - openmpi
c_compiler_version: 
  - 12
fortran_compiler_version:
  - 12
cxx_compiler_version:
  - 12
c_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
cxx_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
fortran_compiler:
  - if: linux
    then: gfortran
  - if: osx
    then: gfortran
